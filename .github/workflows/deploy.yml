on:
  push:
    branches: [ main ]
name: 🚀 Deploy file on SFTP
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2

      - name: Use Node.js 14
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:

           server: '194.163.140.1'
           username: 'mainmakdevdigita'
           password: 'O76jRTHQAP=C'
           server-dir: /public_html/
           exclude: |
             */.git
             */.git/**
             */node_modules/*
             */public/*
             */vendor/*